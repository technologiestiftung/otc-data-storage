# Migration `20201105132122-detection-with-mongo-id`

This migration has been generated by ff6347 at 11/5/2020, 2:21:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "Detection" ADD COLUMN     "mongoId" TEXT

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Area" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "mongoId" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "street" TEXT NOT NULL,
    "streetId" TEXT NOT NULL,
    "x1" REAL NOT NULL,
    "y1" REAL NOT NULL,
    "x2" REAL NOT NULL,
    "y2" REAL NOT NULL,
    "line" TEXT NOT NULL,
    "direction" TEXT NOT NULL,
    "recordingId" INTEGER,

    FOREIGN KEY ("recordingId") REFERENCES "Recording"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Area" ("id", "createdAt", "street", "streetId", "x1", "y1", "x2", "y2", "line", "direction", "recordingId", "mongoId") SELECT "id", "createdAt", "street", "streetId", "x1", "y1", "x2", "y2", "line", "direction", "recordingId", "mongoId" FROM "Area";
DROP TABLE "Area";
ALTER TABLE "new_Area" RENAME TO "Area";
CREATE UNIQUE INDEX "Area.mongoId_unique" ON "Area"("mongoId");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "Detection.mongoId_unique" ON "Detection"("mongoId")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201104133623-add-mongo-id-to-area..20201105132122-detection-with-mongo-id
--- datamodel.dml
+++ datamodel.dml
@@ -1,20 +1,17 @@
-// This is your Prisma schema file,
-// learn more about it in the docs: https://pris.ly/d/prisma-schema
-
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 // datasource db {
 //   provider = "postgresql"
-//   url = "***"
+//   url = "***"
 // }
 generator client {
-  provider             = "prisma-client-js"
-  experimentalFeatures = ["connectOrCreate"]
+  provider        = "prisma-client-js"
+  previewFeatures = ["connectOrCreate"]
 }
 // generator dbml {
 //   provider = "prisma-dbml-generator"
@@ -58,8 +55,9 @@
 // https://opendatacam.github.io/opendatacam/apidoc/#api-Area
 // curl "http://192.168.1.210:8080/counter/areas"
 model Area {
   id        Int      @id @default(autoincrement())
+  mongoId   String   @unique
   createdAt DateTime @default(now())
   street    String
   // manually
   streetId  String // or Int?
@@ -81,9 +79,8 @@
   direction   String
   detections  Detection[]
   recording   Recording?  @relation(fields: [recordingId], references: [id])
   recordingId Int?
-  mongoId     String?
 }
 // is there also a detection object which holds all the objects
 // of every frame detected?
@@ -99,8 +96,9 @@
   area            Area?          @relation(fields: [areaId], references: [id])
   areaId          Int?
   detectionType   DetectionType? @relation(fields: [detectionTypeId], references: [id])
   detectionTypeId Int?
+  mongoId         String?        @unique
 }
 model DetectionType {
   id           Int          @id @default(autoincrement())
```
