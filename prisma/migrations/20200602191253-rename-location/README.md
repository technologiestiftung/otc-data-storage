# Migration `20200602191253-rename-location`

This migration has been generated by fabianmoronzirfas at 6/2/2020, 7:12:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Camera" (
"active" BOOLEAN NOT NULL  ,"angle" TEXT   ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"geom" TEXT   ,"hardware" TEXT   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"location" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,"owner" TEXT   ,"software" TEXT   ,"softwareHistory" TEXT   ,"timezone" INTEGER NOT NULL  ,"transformationMatrix" TEXT   )

INSERT INTO "quaint"."new_Camera" ("active", "angle", "createdAt", "geom", "hardware", "id", "name", "owner", "software", "softwareHistory", "timezone", "transformationMatrix") SELECT "active", "angle", "createdAt", "geom", "hardware", "id", "name", "owner", "software", "softwareHistory", "timezone", "transformationMatrix" FROM "quaint"."Camera"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Camera";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Camera" RENAME TO "Camera";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200602184737-init..20200602191253-rename-location
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 generator client {
   provider = "prisma-client-js"
@@ -14,9 +14,9 @@
   id                   Int                  @default(autoincrement()) @id
   createdAt            DateTime             @default(now())
   active               Boolean
   name                 String
-  loction              String
+  location              String
   transformationMatrix String?
   owner                String?
   timezone             Int
   software             String?
```
