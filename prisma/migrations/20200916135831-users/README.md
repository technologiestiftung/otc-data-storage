# Migration `20200916135831-users`

This migration has been generated by fabianmoronzirfas at 9/16/2020, 1:58:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  ,"password" TEXT NOT NULL  )

CREATE UNIQUE INDEX "quaint"."User.name" ON "User"("name")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200603153312-rename-fields-plural..20200916135831-users
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 generator client {
   provider = "prisma-client-js"
@@ -15,11 +15,11 @@
   createdAt            DateTime             @default(now())
   active               Boolean
   name                 String
   location             String
+  timezone             Int
   transformationMatrix String?
   owner                String?
-  timezone             Int
   software             String?
   softwareHistory      String?
   hardware             String?
   // Geometry(Point, 4326)
@@ -46,11 +46,11 @@
   y2        Float
   // Geometry(Line, 4326)
-  line      String
+  line       String
   // Geometry(Line, 4326)
-  direction String
+  direction  String
   detections Detection[]
 }
 model Detection {
@@ -66,13 +66,13 @@
   y               Float
 }
 model DetectionType {
-  id          Int          @default(autoincrement()) @id
-  label       String
-  description String?
+  id           Int          @default(autoincrement()) @id
+  label        String
+  description  String?
   detections   Detection[]
-  trajectories  Trajectory[]
+  trajectories Trajectory[]
 }
 model Trajectory {
   id         Int    @default(autoincrement()) @id
@@ -111,4 +111,11 @@
   createdAt  DateTime @default(now())
   recordedAt DateTime
   value      Float
 }
+
+model User {
+  id       Int    @default(autoincrement()) @id
+  name     String @unique
+  password String
+
+}
```
