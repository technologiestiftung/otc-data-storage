# Migration `20201103173722-remove-counter-from-detenction-type`

This migration has been generated by fabianmoronzirfas at 11/3/2020, 6:37:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Counter" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "street" TEXT NOT NULL,
    "streetId" TEXT NOT NULL,
    "x1" REAL NOT NULL,
    "y1" REAL NOT NULL,
    "x2" REAL NOT NULL,
    "y2" REAL NOT NULL,
    "line" TEXT NOT NULL,
    "direction" TEXT NOT NULL,
    "recordingId" INTEGER,

    FOREIGN KEY ("recordingId") REFERENCES "Recording"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Counter" ("id", "createdAt", "street", "streetId", "x1", "y1", "x2", "y2", "line", "direction", "recordingId") SELECT "id", "createdAt", "street", "streetId", "x1", "y1", "x2", "y2", "line", "direction", "recordingId" FROM "Counter";
DROP TABLE "Counter";
ALTER TABLE "new_Counter" RENAME TO "Counter";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201103173148-cleanup..20201103173722-remove-counter-from-detenction-type
--- datamodel.dml
+++ datamodel.dml
@@ -2,14 +2,14 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 // datasource db {
 //   provider = "postgresql"
-//   url = "***"
+//   url = "***"
 // }
 generator client {
   provider = "prisma-client-js"
@@ -72,19 +72,18 @@
   // Geometry(Line, 4326)
   // createa manually by try and error :D
   // if we have a geom on the camera
   // we can calculate that line string from the coordinates
-  line            String
+  line        String
   // should be actually
   // - bidirectional
   // - left
   // - right
-  direction       String
-  detections      Detection[]
-  recording       Recording?     @relation(fields: [recordingId], references: [id])
-  recordingId     Int?
-  detectionType   DetectionType? @relation(fields: [detectionTypeId], references: [id])
-  detectionTypeId Int?
+  direction   String
+  detections  Detection[]
+  recording   Recording?  @relation(fields: [recordingId], references: [id])
+  recordingId Int?
+
 }
 // is there also a detection object which holds all the objects
 // of every frame detected?
@@ -106,11 +105,11 @@
 model DetectionType {
   id           Int          @id @default(autoincrement())
   label        String       @unique
   description  String?
-  counters     Counter[]
   trajectories Trajectory[]
   detections   Detection[]
+
 }
 // is a collection of detections
 // replace by the track object
```
