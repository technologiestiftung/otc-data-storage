# Migration `20201105142509-add-recording-to-trajectories`

This migration has been generated by ff6347 at 11/5/2020, 3:25:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Trajectory" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "trajectory" TEXT NOT NULL,
    "geom" TEXT NOT NULL,
    "start" DATETIME NOT NULL,
    "end" DATETIME NOT NULL,
    "detectionTypeId" INTEGER,
    "cameraId" INTEGER,
    "recordingId" INTEGER,

    FOREIGN KEY ("detectionTypeId") REFERENCES "DetectionType"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("cameraId") REFERENCES "Camera"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("recordingId") REFERENCES "Recording"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Trajectory" ("id", "cameraId", "trajectory", "geom", "detectionTypeId", "start", "end") SELECT "id", "cameraId", "trajectory", "geom", "detectionTypeId", "start", "end" FROM "Trajectory";
DROP TABLE "Trajectory";
ALTER TABLE "new_Trajectory" RENAME TO "Trajectory";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201105140623-detections-in-recordings..20201105142509-add-recording-to-trajectories
--- datamodel.dml
+++ datamodel.dml
@@ -1,12 +1,12 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 // datasource db {
 //   provider = "postgresql"
-//   url = "***"
+//   url = "***"
 // }
 generator client {
   provider        = "prisma-client-js"
@@ -42,16 +42,17 @@
 }
 model Recording {
-  id             Int         @id @default(autoincrement())
-  createdAt      DateTime    @default(now())
+  id             Int          @id @default(autoincrement())
+  createdAt      DateTime     @default(now())
   recordingStart DateTime
   recordingEnd   DateTime
   areas          Area[]
-  camera         Camera?     @relation(fields: [cameraId], references: [id])
+  camera         Camera?      @relation(fields: [cameraId], references: [id])
   cameraId       Int?
   detections     Detection[]
+  trajectories   Trajectory[]
 }
 // Area should be equivalent to Area
 // https://opendatacam.github.io/opendatacam/apidoc/#api-Area
@@ -100,11 +101,11 @@
   detectionType   DetectionType? @relation(fields: [detectionTypeId], references: [id])
   detectionTypeId Int?
   mongoId         String
-  @@unique([id, mongoId])
   Recording   Recording? @relation(fields: [recordingId], references: [id])
   recordingId Int?
+  @@unique([id, mongoId])
 }
 model DetectionType {
   id           Int          @id @default(autoincrement())
@@ -134,8 +135,10 @@
   detectionType   DetectionType? @relation(fields: [detectionTypeId], references: [id])
   detectionTypeId Int?
   camera          Camera?        @relation(fields: [cameraId], references: [id])
   cameraId        Int?
+  Recording       Recording?     @relation(fields: [recordingId], references: [id])
+  recordingId     Int?
 }
 model Weather {
   id            Int      @id @default(autoincrement())
```
